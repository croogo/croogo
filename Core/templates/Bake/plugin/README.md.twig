# {{ plugin }} plugin for Croogo

## Installation

You can install this plugin into your Croogo application using [composer](https://getcomposer.org).

The recommended way to install composer packages is:

```
composer require {{ package }}
```
